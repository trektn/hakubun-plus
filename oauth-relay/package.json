{
  "name": "mal-oauth-relay",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that relays MyAnimeList's OAuth redirect to the Hakubun+ desktop app via a custom URI scheme.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
