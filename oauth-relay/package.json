{
  "name": "mal-oauth-relay",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that shows MyAnimeList's OAuth authorization code so it can be pasted into Hakubun+'s PIN field.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
